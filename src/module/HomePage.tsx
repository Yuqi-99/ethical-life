import { HomeSection } from '../components/ImageSequenceHeader';

export const HomePage = () => {
	return (
		<div className='relative flex min-h-screen w-full max-w-[inherit] flex-col items-center'>
			<HomeSection />
		</div>
	);
};
